{
  "intro": [
    "Welcome to psql 8.0.2, the PostgreSQL interactive terminal.",
    "The first version I ran, on Windows, in 2005. It still starts.",
    "",
    "Type:  \\h for help with SQL commands",
    "       \\? for help with psql commands",
    "       \\q to quit — though there is nowhere else to go"
  ],
  "steps": [
    { "cmd": "select version();", "label": "version()", "kind": "text", "lines": ["                              version", "------------------------------------------------------------------", " PostgreSQL 8.0.2 — kept around out of respect, not necessity"], "footer": "(1 row)" },
    { "cmd": "\\h", "label": "\\h", "kind": "text", "lines": ["Available commands:", "  \\du                        roles — who is typing this", "  SELECT * FROM experience   where twenty years went", "  SELECT * FROM projects     things that escaped localhost", "  SELECT * FROM stack        tools, honestly used", "  now()                      current status", "  \\c contact                 open a new connection"], "footer": "" },
    { "cmd": "\\du", "label": "\\du", "kind": "roles", "footer": "(2 roles)" },
    { "cmd": "SELECT * FROM experience ORDER BY started DESC;", "label": "experience", "kind": "experience", "footer": "(6 rows)" },
    { "cmd": "SELECT * FROM projects;", "label": "projects", "kind": "projects", "footer": "(3 rows)" },
    { "cmd": "SELECT * FROM stack;", "label": "stack", "kind": "stack", "footer": "(4 rows)" },
    { "cmd": "SELECT now(), status;", "label": "now()", "kind": "text", "lines": ["         now          |                        status", "----------------------+------------------------------------------------------", " 2026-08-14 09:12:03  | at PlanetScale; pager attached, mostly quiet"], "footer": "(1 row)" },
    { "cmd": "\\c contact", "label": "\\c contact", "kind": "links", "footer": "You are now connected to database \"contact\" as user \"sebastian\"." }
  ]
}
